
Questions:
->Run the program from main and use the two data structures or run them separately?
->In the scavenger hunt display the item when user wants "get" or just time it?
actually print it

separate them into classes->new project, write observations